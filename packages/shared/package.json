{
  "name": "@finsecure/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Everything the Banking and Insurance apps must agree on: the localStorage schema, the KYC record, masking rules, validation, the OTP component and the UI kit. Requirement 12 (insurance never re-asks for KYC the customer already gave banking) is only true because both apps read the SAME record from here.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./storage": "./src/storage/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.1"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "lucide-react": "^1.33.0",
    "react-router-dom": "^7.1.1",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@akku-work/consent-auth": "0.2.0-beta.2",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "next-themes": "^0.4.6",
    "radix-ui": "^1.6.7",
    "sonner": "^2.0.8",
    "tailwind-merge": "^3.6.0"
  }
}
